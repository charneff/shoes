<%= f.label :brand, "Brand:" %>
    <%= f.collection_select :brand_id, Brand.all, :id, :name, prompt: true %><br><br>
    <%= f.fields_for :brand do |b| %>
      <%= render partial: 'brands/fields', locals: {f: b} %>
    <% end %><br><br>